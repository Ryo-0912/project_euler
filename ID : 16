line = []
1000.times do |n|
  line.push(2**(n+1))
end
last_line = line.last.to_s.chars

puts last_line.map { |x| x.to_i }.sum

# 別解

puts (2**1000).to_s.chars.map{ |x| x.to_i }.sum  # こんな簡単に階乗が表現できるみたい
